const config = {
  mode: "jit",
  content: ["./src/**/*.{html,js,svelte,ts}"],
  theme: {
    borderRadius: {
      sm: "1px",
      md: "5px",
      lg: "6px",
    },
  },
};

module.exports = config;
